#include "Layout.h"

namespace UI
{
    namespace layout
    {
        Layout::Layout(UI::Container *container) : container(container)
        {
        }

    } // namespace layout
} // namespace UI
