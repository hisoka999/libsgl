#pragma once

#include <cstdint>

namespace utils::temperature
{

    constexpr float convert_c2f(const float celsius);
    constexpr float convert_f2c(const float fahrenheit);
} // namespace utils::temperature
